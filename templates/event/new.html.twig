{% extends 'layout_admin.html.twig' %}

{% block title %}Nouvel événement{% endblock %}

{% block body %}
    <div class="mt-5 container-xl container-lg container-fluid">
        <h1 class="text-center mt-3 mb-4">Créer un nouvel événement</h1>
        <div class="formular-create p-4">
            {{ include('event/_form.html.twig') }}
        </div>
        <div class="flex d-flex justify-content-around">
            <a class="btn btn-blue m-5" href="{{ path('event_index') }}">
                <i class="far fa-arrow-alt-circle-left"></i> Liste de vos événements</a>
        </div>
    </div>
{% endblock %}
