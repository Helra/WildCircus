{% extends 'layout_admin.html.twig' %}

{% block title %}Edit Spectacle{% endblock %}

{% block body %}
    <div class="mt-5 container-xl container-lg container-fluid">
        <h1 class="text-center mt-3 mb-4">Modifier un spectacle</h1>
        <div class="formular-create p-4">
            {{ include('spectacle/_form.html.twig', {'button_label': 'Enregistrer'}) }}
        </div>
        <div class="row justify-content-between">
            <div class="col col-lg-4">
                <a class="btn btn-blue m-5" href="{{ path('spectacle_index') }}">
                    <i class="far fa-arrow-alt-circle-left"></i> Liste de vos spectacles
                </a>
            </div>
            <div class="col col-lg-3">
                {{ include('spectacle/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}
